lib=CTkImage(Image.open("icons\library icon.png"),size=(40,40))
# l=CTkLabel(navbar,text="",image=lib)
# l.pack(side=LEFT,padx=10)

# lib=CTkImage(Image.open("icons\library icon.png"),size=(40,40))
# l=CTkLabel(navbar,text="",image=lib)
# l.pack(side=RIGHT,padx=10)